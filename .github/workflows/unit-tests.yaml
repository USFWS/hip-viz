# This file was generated with AI assistance using Claude Opus 4.8 on August 10,
# 2026.

# Quick unit testing on every push/PR

on:
  push:
    branches: [ main, master ]
  pull_request:

name: unit-tests.yaml

permissions: read-all

jobs:
  unit-tests:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
      
      - uses: r-lib/actions/setup-r@v2
      
      - name: Install dependencies
        run: install.packages(c("testthat", "scales", "here"))
        shell: Rscript {0}
        
      - name: Run unit tests
        run: testthat::test_dir("tests/testthat", filter = "magic_number", stop_on_failure = TRUE)
        shell: Rscript {0}

